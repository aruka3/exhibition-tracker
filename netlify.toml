[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

**3. 保存したら、ターミナルで：**
```
git add netlify.toml
git commit -m "Add netlify config"
git push